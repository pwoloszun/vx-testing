import { render } from '@testing-library/vue';
import userEvent from '@testing-library/user-event';

import StyledText from '../StyledText.vue';

describe('StyledText.vue', () => {

  // TODO
  xit('should TODO', () => {
  });

});
